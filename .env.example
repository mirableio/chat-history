CHAT_HISTORY_DATA_DIR=data

# Provider exports
# Accepts either:
# - directory path (will use <dir>/conversations.json)
# - direct path to conversations.json
CHAT_HISTORY_CHATGPT_PATH=
CHAT_HISTORY_CLAUDE_PATH=

# Optional explicit settings DB path (defaults to $CHAT_HISTORY_DATA_DIR/settings.db)
CHAT_HISTORY_SETTINGS_DB_PATH=

# Optional semantic search
# OPENAI_API_KEY=
# OPENAI_ORGANIZATION=
# CHAT_HISTORY_OPENAI_ENABLED=true # default: false
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
